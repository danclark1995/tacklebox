/**
 * useSearch Hook — Stub
 * Will provide search functionality with debouncing.
 * Implementation in Phase 1D.
 */

export default function useSearch() {
  return {
    query: '',
    results: null,
    isSearching: false,
    setQuery: () => {},
    search: async () => {},
  }
}
