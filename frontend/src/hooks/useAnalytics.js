/**
 * useAnalytics Hook — Stub
 * Will provide analytics data to dashboard components.
 * Implementation in Phase 1D.
 */

export default function useAnalytics() {
  return {
    data: null,
    isLoading: true,
    error: null,
    filters: {},
    setFilters: () => {},
  }
}
